PORT=2000

# Restrict in production to your frontend's origin, e.g. https://recon.yourdomain.com
CORS_ORIGIN=*

# Shared key required on all /api/scan/* requests when set. Leave blank to
# disable auth (fine for local dev, not recommended once this is on a public IP).
API_KEY=
